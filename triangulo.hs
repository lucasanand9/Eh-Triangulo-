-- A funçao deve receber 3 valores. 
-- Com esses valores, ela deve calcular
-- se é possivel a existencia de um triangulo

soma a b = a + b

triangulo a b c = (soma a b) > c && (soma b c) > a && (soma c a) > c